[package]
name = "outbound-proxy"
version = "0.1.0"
edition = "2021"
description = "Guest-side SOCKS5 proxy that forwards connections via vsock"

[dependencies]
libc = "0.2"

[profile.release]
opt-level = "s"
lto = true
strip = true
panic = "abort"
